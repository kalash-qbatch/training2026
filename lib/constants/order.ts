export const TAX_RATE = 0.1; // 10%

/** Default initial page number */
export const DEFAULT_PAGE = 1;

/** General fallback page size */
export const PAGE_SIZE = 8;

/** Rows per page for table views (e.g. Admin Orders, Admin Products) */
export const TABLE_PAGE_SIZE = 12;
export const TABLE_INITIAL_PAGE = 1;

/** Items per page for card/grid views (e.g. Products listing, User Orders Cards) */
export const CARD_PAGE_SIZE = 8;
export const CARD_INITIAL_PAGE = 1;

/** Notifications loaded per batch/page */
export const NOTIFICATION_PAGE_SIZE = 8;
export const NOTIFICATION_INITIAL_PAGE = 1;

/** Polling interval for deployed environment (10 seconds) */
export const NOTIFICATION_POLL_INTERVAL_MS = 10_000;
